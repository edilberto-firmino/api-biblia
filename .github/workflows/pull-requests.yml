# name: pull requests

# on:
#   pull_request_target:
#     types: [opened]

# permissions:
#   pull-requests: write

# jobs:
#   uneditable:
#     uses: laravel/.github/.github/workflows/pull-requests.yml@main

on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 159.203.28.243
        username: master_btyykbvgkg
        password: vydbzNycYda7
